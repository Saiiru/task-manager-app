pipeline {
    agent any

    environment {
        DATABASE_URL = credentials('DATABASE_URL')
        DOCKERHUB_CREDENTIALS = credentials('dockerhub-credentials')
        GITHUB_TOKEN = credentials('github-token')
    }

    stages {
        stage('Checkout') {
            steps {
                script {
                    if (env.BRANCH_NAME == 'dev') {
                        git branch: 'dev', url: 'https://github.com/saiiru/task-manager-app.git', credentialsId: 'github-token'
                    } else {
                        git branch: 'main', url: 'https://github.com/saiiru/task-manager-app.git', credentialsId: 'github-token'
                    }
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', 'dockerhub-credentials') {
                        sh 'docker buildx create --use'
                        sh 'docker buildx build --platform linux/amd64 -t saiiru/task-manager-backend:latest --push .'
                    }
                }
            }
        }

        stage('Run Tests') {
            steps {
                script {
                    docker.image('saiiru/task-manager-backend:latest').inside {
                        sh 'go test ./...'
                    }
                }
            }
        }

        stage('Push Docker Image') {
            when {
                branch 'dev'
            }
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', 'dockerhub-credentials') {
                        sh 'docker buildx build --platform linux/amd64 -t saiiru/task-manager-backend:dev --push .'
                    }
                }
            }
        }

        stage('Deploy to Dev Kubernetes') {
            when {
                branch 'dev'
            }
            steps {
                script {
                    // Apply Kubernetes resources for Dev environment
                    sh 'kubectl apply -f ci-cd/kubernetes/secret.yaml'
                    sh 'kubectl apply -f ci-cd/kubernetes/deployment-dev.yaml'
                    sh 'kubectl apply -f ci-cd/kubernetes/service-dev.yaml'
                }
            }
        }

        stage('Promote to Prod') {
            when {
                branch 'main'
            }
            steps {
                script {
                    // Push to Docker Hub and apply production Kubernetes resources
                    docker.withRegistry('https://index.docker.io/v1/', 'dockerhub-credentials') {
                        sh 'docker buildx build --platform linux/amd64 -t saiiru/task-manager-backend:latest --push .'
                    }
                    sh 'kubectl apply -f ci-cd/kubernetes/secret.yaml'
                    sh 'kubectl apply -f ci-cd/kubernetes/deployment-prod.yaml'
                    sh 'kubectl apply -f ci-cd/kubernetes/service-prod.yaml'
                }
            }
        }
    }
}